import { Outlet } from "react-router-dom"

const RecordLayout = () => {
    return (
        <div className="records-layout">
            <Outlet />
        </div>
    );
}

export default RecordLayout


